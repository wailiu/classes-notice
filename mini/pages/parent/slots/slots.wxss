.has-bar {
  padding-bottom: 160rpx;
}

.head {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.head-course {
  font-size: 36rpx;
  font-weight: 700;
  letter-spacing: 1rpx;
}

/* ---------- 预约方式 ---------- */
.mode-tabs {
  display: flex;
  background: #f1ede6;
  border-radius: 999rpx;
  padding: 6rpx;
  margin-bottom: 24rpx;
}

.mode-tab {
  flex: 1;
  text-align: center;
  font-size: 28rpx;
  padding: 14rpx 0;
  border-radius: 999rpx;
  color: #7a7266;
}

.mode-tab.active {
  background: #fff;
  color: #e8542c;
  font-weight: 600;
  box-shadow: 0 4rpx 12rpx rgba(96, 74, 52, 0.1);
}

/* ---------- 时段筛选(仅辅助浏览,不限制勾选) ---------- */
.filter-bar {
  display: flex;
  flex-direction: row;
  white-space: nowrap;
  margin-bottom: 18rpx;
}

.chip {
  flex: 1 0 auto;
  box-sizing: border-box;
  border: 2rpx solid #e9e2d6;
  border-radius: 999rpx;
  padding: 10rpx 26rpx;
  margin-right: 14rpx;
  text-align: center;
  font-size: 24rpx;
  color: #7a7266;
  background: #fff;
}

.chip:last-child {
  margin-right: 0;
}

.chip.active {
  border-color: #e8542c;
  background: #fdf0e9;
  color: #e8542c;
  font-weight: 600;
}

.list-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12rpx;
}

.list-tip {
  font-size: 24rpx;
  color: #948b7d;
}

.select-all {
  font-size: 24rpx;
  color: #e8542c;
  font-weight: 600;
}

.sub-title {
  font-size: 26rpx;
  font-weight: 600;
  letter-spacing: 1rpx;
  margin: 24rpx 0 14rpx;
}

.sub-title:first-child {
  margin-top: 0;
}

.sub-tip {
  font-size: 22rpx;
  font-weight: 400;
  color: #948b7d;
  margin-left: 12rpx;
}

/* ---------- 课次勾选 ---------- */
.lesson-row {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 20rpx 12rpx;
  border-radius: 16rpx;
  border-bottom: 1rpx solid #f3efe8;
}

.lesson-row.disabled {
  opacity: 0.55;
}

.lesson-row.checked {
  background: #fdf4ee;
  border-bottom-color: transparent;
}

/* ---------- 每周课程勾选(长期预约) ---------- */
.pattern-row {
  display: flex;
  align-items: center;
  gap: 16rpx;
  border: 2rpx solid #e9e2d6;
  border-radius: 16rpx;
  padding: 20rpx;
  margin-bottom: 14rpx;
}

.pattern-row.checked {
  border-color: #e8542c;
  background: #fdf4ee;
}

.checkbox {
  width: 38rpx;
  height: 38rpx;
  border: 2rpx solid #d8d0c2;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  color: #fff;
  flex-shrink: 0;
}

.checkbox.on {
  background: #e8542c;
  border-color: #e8542c;
}

.lesson-info {
  flex: 1;
}

.lesson-date {
  font-size: 28rpx;
  font-weight: 500;
}

.lesson-time {
  font-size: 22rpx;
  color: #948b7d;
  margin-top: 6rpx;
}

/* ---------- 长期预约 ---------- */
.range-opts {
  display: flex;
  gap: 16rpx;
  margin-bottom: 8rpx;
}

.range-opt {
  flex: 1;
  text-align: center;
  border: 2rpx solid #e9e2d6;
  border-radius: 999rpx;
  padding: 16rpx 0;
  font-size: 28rpx;
  color: #7a7266;
}

.range-opt.active {
  border-color: #e8542c;
  background: #fdf0e9;
  color: #e8542c;
  font-weight: 600;
}

.lt-summary {
  background: #faf6f0;
  border-radius: 16rpx;
  padding: 22rpx;
  margin-top: 18rpx;
  font-size: 26rpx;
}

.hl {
  color: #e8542c;
  font-weight: 700;
}

.lt-warn {
  margin-top: 14rpx;
  font-size: 24rpx;
  color: #d9483b;
}

/* ---------- 底部提交栏 ---------- */
.bottom-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  background: #fff;
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 18rpx 28rpx calc(18rpx + env(safe-area-inset-bottom));
  box-shadow: 0 -6rpx 20rpx rgba(96, 74, 52, 0.08);
}

.bar-info {
  flex: 1;
  font-size: 24rpx;
  color: #7a7266;
}

.bar-btn {
  width: 260rpx;
  margin: 0;
}

/* ---------- 结果面板 ---------- */
.result-mask {
  position: fixed;
  inset: 0;
  background: rgba(43, 36, 28, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 99;
}

.result-panel {
  width: 620rpx;
  max-height: 75vh;
  background: #fff;
  border-radius: 28rpx;
  padding: 36rpx 32rpx;
  display: flex;
  flex-direction: column;
}

.result-title {
  font-size: 34rpx;
  font-weight: 700;
  text-align: center;
  letter-spacing: 1rpx;
}

.result-summary {
  font-size: 26rpx;
  text-align: center;
  color: #7a7266;
  margin: 18rpx 0;
}

.result-list {
  flex: 1;
  max-height: 40vh;
}

.result-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-size: 24rpx;
  padding: 14rpx 0;
  border-bottom: 1rpx solid #f3efe8;
}

.ok-text {
  color: #1f9d55;
  font-weight: 600;
}

.fail-text {
  color: #d9483b;
  max-width: 260rpx;
  text-align: right;
}

.result-btn {
  margin-top: 28rpx;
}
